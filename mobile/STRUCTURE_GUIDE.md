# Mobile App Structure Visualization

## 📂 Complete Project Structure

```
mobile/
│
├── 📱 app/
│   ├── 📄 build.gradle.kts                    [UPDATED] Dependencies added
│   │
│   └── src/
│       ├── main/
│       │   ├── 📝 AndroidManifest.xml         [UPDATED] Permissions & Activities
│       │   │
│       │   ├── java/com/example/miniapp_mobile/
│       │   │   │
│       │   │   ├── 📦 data/                   [NEW PACKAGE]
│       │   │   │   ├── api/
│       │   │   │   │   ├── AuthApiService.kt      ← Retrofit interface
│       │   │   │   │   └── RetrofitClient.kt      ← HTTP client config
│       │   │   │   │
│       │   │   │   ├── model/
│       │   │   │   │   ├── RegisterRequest.kt     ← Request DTO
│       │   │   │   │   ├── AuthResponse.kt        ← Response DTO
│       │   │   │   │   └── MessageResponse.kt     ← Message DTO
│       │   │   │   │
│       │   │   │   └── repository/
│       │   │   │       └── AuthRepository.kt      ← Data layer
│       │   │   │
│       │   │   ├── 📦 ui/                     [NEW PACKAGE]
│       │   │   │   └── auth/
│       │   │   │       ├── RegisterActivity.kt    ← Registration UI
│       │   │   │       └── RegisterViewModel.kt   ← Business logic
│       │   │   │
│       │   │   └── MainActivity.kt            [UPDATED] Entry point
│       │   │
│       │   └── res/
│       │       ├── drawable/                  [NEW DRAWABLES]
│       │       │   ├── auth_background.xml        ← Gradient bg
│       │       │   └── brand_icon_background.xml  ← Icon border
│       │       │
│       │       ├── layout/
│       │       │   ├── activity_main.xml          [EXISTING]
│       │       │   └── activity_register.xml      [NEW] Registration form
│       │       │
│       │       └── values/
│       │           ├── colors.xml             [UPDATED] Full palette
│       │           ├── strings.xml            [EXISTING]
│       │           └── themes.xml             [EXISTING]
│       │
│       ├── test/                              [EXISTING]
│       └── androidTest/                       [EXISTING]
│
├── 📚 Documentation/
│   ├── 📄 README.md                           [NEW] Full documentation
│   ├── 📄 QUICKSTART.md                       [NEW] 5-min setup guide
│   ├── 📄 MOBILE_CONFIG.md                    [NEW] Configuration guide
│   └── 📄 IMPLEMENTATION_SUMMARY.md           [NEW] This summary
│
└── 📄 build.gradle.kts                        [EXISTING]
    settings.gradle.kts                        [EXISTING]
    gradlew                                    [EXISTING]
    gradlew.bat                                [EXISTING]
```

## 🔄 Data Flow Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        RegisterActivity                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  • User enters data (first name, last name, email, etc)  │  │
│  │  • Clicks "Create account" button                        │  │
│  │  • Observes ViewModel state changes                      │  │
│  │                                                           │  │
│  └─────────────────────┬────────────────────────────────────┘  │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       RegisterViewModel                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  • Validates form fields                                 │  │
│  │  • Updates LiveData states (Loading, Success, Error)     │  │
│  │  • Calls repository methods via Coroutines               │  │
│  │                                                           │  │
│  └─────────────────────┬────────────────────────────────────┘  │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        AuthRepository                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  • Abstracts data source                                 │  │
│  │  • Handles API calls via Retrofit                        │  │
│  │  • Wraps results in Result<T> type                       │  │
│  │                                                           │  │
│  └─────────────────────┬────────────────────────────────────┘  │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       AuthApiService                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  • Retrofit interface                                    │  │
│  │  • Defines API endpoints                                 │  │
│  │  • POST /auth/register                                   │  │
│  │                                                           │  │
│  └─────────────────────┬────────────────────────────────────┘  │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       RetrofitClient                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  • HTTP client configuration                             │  │
│  │  • Base URL: http://10.0.2.2:8080/api/                  │  │
│  │  • Logging interceptor                                   │  │
│  │  • Gson converter                                        │  │
│  │                                                           │  │
│  └─────────────────────┬────────────────────────────────────┘  │
└────────────────────────┼───────────────────────────────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │   INTERNET   │
                  └──────┬───────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Backend API Server                            │
│                                                                  │
│        POST http://localhost:8080/api/auth/register             │
│                                                                  │
│  Request:                          Response:                    │
│  {                                 {                            │
│    "firstName": "John",              "message": "Success"       │
│    "lastName": "Doe",              }                            │
│    "email": "john@test.com",                                    │
│    "password": "pass123"                                        │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

## 🎨 UI Component Hierarchy

```
activity_register.xml
│
└── ScrollView (Full screen)
    └── ConstraintLayout (Padded container)
        │
        ├── LinearLayout (Brand Section)
        │   ├── TextView (Icon "M")
        │   └── TextView (App Name "MiniApp")
        │
        └── MaterialCardView (Registration Card)
            └── LinearLayout (Card Content)
                │
                ├── TextView (Title: "Create Account")
                ├── TextView (Subtitle)
                │
                ├── MaterialCardView (Error Card - Hidden by default)
                │   └── TextView (Error Message)
                │
                ├── LinearLayout (Name Row)
                │   ├── TextInputLayout (First Name)
                │   │   └── TextInputEditText
                │   └── TextInputLayout (Last Name)
                │       └── TextInputEditText
                │
                ├── TextInputLayout (Email)
                │   └── TextInputEditText
                │
                ├── LinearLayout (Password Row)
                │   ├── TextInputLayout (Password)
                │   │   └── TextInputEditText
                │   └── TextInputLayout (Confirm Password)
                │       └── TextInputEditText
                │
                ├── MaterialButton (Register Button)
                ├── ProgressBar (Loading - Hidden by default)
                │
                └── LinearLayout (Footer)
                    ├── TextView ("Already have an account?")
                    └── TextView ("Sign in" - Clickable)
```

## 🎭 State Management Flow

```
RegisterViewModel States:
│
├── RegisterState.Idle
│   ├── UI: Normal state
│   ├── Inputs: Enabled
│   └── Actions: User can type and submit
│
├── RegisterState.Loading
│   ├── UI: Show loading indicator
│   ├── Inputs: Disabled
│   └── Button: Disabled with loading text
│
├── RegisterState.Success(message)
│   ├── UI: Show success Snackbar
│   ├── Form: Clear all fields
│   └── Inputs: Re-enabled
│
└── RegisterState.Error(message)
    ├── UI: Show error card
    ├── Error Card: Display message
    └── Inputs: Re-enabled
```

## 🔌 Network Configuration Matrix

```
┌─────────────────┬────────────────────────┬──────────────────────┐
│   Environment   │      Base URL          │     Configuration    │
├─────────────────┼────────────────────────┼──────────────────────┤
│  Emulator       │ http://10.0.2.2:8080   │  No changes needed   │
│  (Default)      │        /api/           │  Works out of box    │
├─────────────────┼────────────────────────┼──────────────────────┤
│  Physical       │ http://192.168.1.X:    │  Update RetrofitClient│
│  Device         │      8080/api/         │  Use computer IP     │
├─────────────────┼────────────────────────┼──────────────────────┤
│  Production     │ https://yourdomain.com │  Change to HTTPS     │
│                 │        /api/           │  Update manifest     │
└─────────────────┴────────────────────────┴──────────────────────┘
```

## 🛡️ Validation Rules Flowchart

```
User Clicks "Create account"
          │
          ▼
    ┌─────────────┐
    │ First Name? │──No──► Show Error: "First name is required"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │ Last Name?  │──No──► Show Error: "Last name is required"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │   Email?    │──No──► Show Error: "Email is required"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │Valid Email? │──No──► Show Error: "Invalid email format"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │  Password?  │──No──► Show Error: "Password is required"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │  Length≥6?  │──No──► Show Error: "Password must be ≥6 chars"
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │Passwords    │──No──► Show Error: "Passwords do not match"
    │   Match?    │
    └─────────────┘
          │ Yes
          ▼
    ┌─────────────┐
    │  Call API   │
    └─────────────┘
```

## 📊 Dependency Graph

```
┌─────────────────────────────────────────────────────────────────┐
│                         RegisterActivity                         │
│                              (UI)                                │
└────────────────────────┬────────────────────────────────────────┘
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       RegisterViewModel                          │
│                       (Business Logic)                           │
└────────────────────────┬────────────────────────────────────────┘
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        AuthRepository                            │
│                         (Data Layer)                             │
└────────────────────────┬────────────────────────────────────────┘
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       AuthApiService                             │
│                      (API Interface)                             │
└────────────────────────┬────────────────────────────────────────┘
                         │ uses
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       RetrofitClient                             │
│                    (HTTP Configuration)                          │
└─────────────────────────────────────────────────────────────────┘

Supporting Models:
├── RegisterRequest.kt
├── AuthResponse.kt
└── MessageResponse.kt
```

## 🎨 Color System Mapping

```
Web App Colors          →     Mobile App Colors
─────────────────────────────────────────────────────
#0F1A33 (bg dark)       →     @color/background_dark
#0B1428 (bg darker)     →     @color/background_darker
#1A2844 (surface)       →     @color/surface_dark

#4E6BF2 (accent)        →     @color/accent_primary
#5B75F7 (accent strong) →     @color/accent_strong
#6F88FF (accent light)  →     @color/accent_light

#EEF3FF (text)          →     @color/text_primary
#C3CBDE (text sec)      →     @color/text_secondary
#7A8499 (text muted)    →     @color/text_muted
#5A6579 (text hint)     →     @color/text_hint

#E85D75 (error)         →     @color/error
#4CAF50 (success)       →     @color/success
```

## 🔧 Build Configuration

```
build.gradle.kts (Project Level)
├── Gradle: 8.4
├── Kotlin: 1.9.0
└── Android Gradle Plugin: 8.2.0

build.gradle.kts (App Level)
├── compileSdk: 34
├── minSdk: 24
├── targetSdk: 34
│
├── ViewBinding: Enabled
│
└── Dependencies:
    ├── Retrofit: 2.9.0
    ├── OkHttp Logging: 4.11.0
    ├── Coroutines: 1.7.3
    ├── Lifecycle: 2.6.2
    ├── Fragment KTX: 1.6.2
    └── Material: (from libs)
```

## 🚀 Launch Sequence

```
1. App Starts
   └── MainActivity.onCreate()
       └── Launches RegisterActivity

2. RegisterActivity.onCreate()
   ├── Inflates layout (ViewBinding)
   ├── Sets up UI listeners
   └── Observes ViewModel states

3. User Interaction
   ├── Types in fields → LiveData updates
   ├── Clicks button → viewModel.register()
   └── ViewModel validates and calls API

4. API Call (Coroutine)
   ├── Repository.register()
   ├── ApiService.register()
   └── RetrofitClient sends HTTP POST

5. Response Handling
   ├── Success → Update state → Show Snackbar
   └── Error → Update state → Show error card
```

---

## 📝 Quick File Reference

**Need to change API URL?**
→ `RetrofitClient.kt` line 16

**Need to update colors?**
→ `res/values/colors.xml`

**Need to modify UI?**
→ `res/layout/activity_register.xml`

**Need to change validation?**
→ `RegisterViewModel.kt` register() function

**Need to handle API errors?**
→ `AuthRepository.kt` register() function

---

**Visual guide complete! All structures documented. 🎉**
